services:
  - type: static_site
    name: lecture-showcase
    env: node
    buildCommand: npm install --no-audit --no-fund && npm run build && ls -la dist/
    staticPublishPath: dist
    routes:
      - path: /
        index: index.html
      - path: /*
        index: index.html
